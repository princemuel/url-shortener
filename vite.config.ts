import { defineConfig } from 'vite';

// export default defineConfig(({ mode }) => {

//   return {
//     // dev specific config
//     server: { port: 3000 },
//   };
// });

export default defineConfig({});
